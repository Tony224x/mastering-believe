"""[NN] - [Module title] - runnable companion to 01-theory/[NN]-*.md.

Standalone script: python 01-module-template.py
Dependencies: stdlib only (or document them here, e.g. # requires: torch)

Reading time: 15-30 min. Comments explain WHY, not WHAT.
"""


def main() -> None:
    # WHY: each code example must be runnable end-to-end without arguments,
    # so a learner can execute it right after reading the theory module.
    print("Replace with a demo that mirrors the theory module sections.")


if __name__ == "__main__":
    main()
